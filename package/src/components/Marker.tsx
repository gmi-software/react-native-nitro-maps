import type { MarkerProps } from '../types/overlays';

/**
 * Marker overlay child for {@linkcode MapView}.
 *
 * Props are collected by the parent `MapView` and serialized into a
 * {@linkcode MarkerDescriptor} passed to the native map view.
 */
export function Marker(_props: MarkerProps): null {
  return null;
}
